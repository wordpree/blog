import React from "react";
import ImportContactsOutlinedIcon from "@mui/icons-material/ImportContactsOutlined";

const Logo = ({ color = "primary", size = "medium" }) => {
  return <ImportContactsOutlinedIcon color={color} fontSize={size} />;
};

export default Logo;
